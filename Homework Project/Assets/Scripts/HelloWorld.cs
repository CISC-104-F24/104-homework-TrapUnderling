using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello World");
        Debug.LogWarning("WARNING! BE CAREFUL WITH PROCEEDINGS!");
        Debug.LogError("There has been an error. Please try again.");
        //Author: Mason Toothman
        /*
         * How would you import outside images into unity and create separate character dialogues?
         * I have an idea to make a somewhat dark visual novel with heavy undertones depicting mental health; think of it somewhat like Omori as an example, but without the happy-go-lucky parts in a way.
         * I hope to learn more about particles or scripting while in this class.
         */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
